# MEMORY.md

このリポジトリで Claude Code(Anthropic CLI)が **会話を跨いで覚えておく文脈**のインデックス。

実体は OS ユーザディレクトリ配下の auto-memory ストアに分散して置かれている。
このファイルは「何が記録されているか」をリポジトリ側から **見える化** するためのコピー。

> 仕組みの詳細: [docs/04_モデル検証/2026-05-01_M-11_自然会話と役の問題.md](docs/04_モデル検証/2026-05-01_M-11_自然会話と役の問題.md) と
> 個別 memory ファイル(下表)を参照。

## 索引(2026-05-01 時点)

| 種別 | タイトル | 要約 |
| --- | --- | --- |
| feedback | 機械的会話を避ける | 役を演じさせる設計(`role` / `speaking_style` 直書き)は NG。生活の文脈ベースで自然さを出す |
| project | 国籍 9:1 回帰 | 2026-05-01 にユーザ判断で 4/24 議事録設計(日本人 90% / 外国人 10%)に回帰。次回 5/3〜5 MTG で粉川氏に報告必要 |
| project | `personal_context` へのピボット | Codex の役割ベース処方箋(`conversation_profiles`)を一部撤回し、`mood / on_mind / today` スキーマに再設計中 |
| reference | モデル検証ログの保存場所 | LLM の挙動・プロンプト設計の検証は [docs/04_モデル検証/](docs/04_モデル検証/) に M-XX 連番で蓄積する慣行 |

## 4 種類の memory(参考)

| 種別 | 何を入れるか |
| --- | --- |
| **user** | ユーザの役割・好み・知識(プロジェクト固有でない、人物プロファイル) |
| **feedback** | やり方の指摘(なぜそうしたか **Why** + **How to apply** を必ず添える) |
| **project** | 進行中の案件・締切・判断履歴(時間で陳腐化するもの) |
| **reference** | 外部システムへのポインタ(議事録の場所、検証ログの慣行など) |

## 何を入れない / 入れる

入れない(コードや git log で分かる):
- ファイルパス、関数名、アーキテクチャ詳細
- 直近のコミット内容、誰が何を変えたか
- バグの修正レシピ
- [CLAUDE.md](CLAUDE.md) に書かれているプロジェクト共通ルール

入れる(コードから読み取れない、会話で蓄積された個別文脈):
- ユーザの設計上の好み・避けたい方向性
- 議事録間で覆った決定の経緯
- 別 AI(Codex 等)の提案を採用/棄却した判断
- 外部システムの位置情報

## メンテナンス

- 状況が変わったら memory を **更新**(削除・上書き)する。古い memory を信じて動くと事故る
- 「○○を memory に保存して」と一言指示があれば私が適切な type を判断して書き出す
- 「もう古い、忘れて」と指示されたら該当ファイルを削除する

---

**Note**: このファイルはリポジトリで人間が見るためのコピー。auto-memory 本体は
`%USERPROFILE%\.claude\projects\d--Projects-IRIS-company-departments-r-and-d-singulab\memory\`
にあり、Claude Code 起動時にそちらが自動的に context にロードされる。
両者の内容が乖離しないように、変更時は両方を更新する。
