# 01_プロンプトの全体構造

各エージェントの 1 ターンの入力は **3 ブロック** で構成:

```
[System Prompt]
= ペルソナ(4.2 エージェント属性)
+ 1階情報(現在いる place)
+ 2階情報(景気)

[User Prompt]
= 自分が知覚した近傍エージェントの直近発話
+ 出力形式の指示(JSON)
```

## 1 ターンのデータフロー図

```mermaid
graph LR
    subgraph Persona["ペルソナ"]
        P1[性別/年齢]
        P2[国籍]
        P3[場所/規模]
    end
    subgraph Floor1["1階(物)"]
        F1[current_place]
    end
    subgraph Floor2["2階(環境)"]
        F2[economy]
    end
    subgraph Floor3["3階の法則<br/>(Python 側で制御)"]
        FL[到達距離 + 遅延<br/>+ 認知限界]
    end

    All[全エージェントの<br/>発話] --> FL
    FL --> Visible[届いた発話のみ]

    P1 --> SP[System Prompt]
    P2 --> SP
    P3 --> SP
    F1 --> SP
    F2 --> SP

    Visible --> UP[User Prompt]
    JS[JSON 出力指示] --> UP

    SP --> LLM[LLM 呼び出し]
    UP --> LLM
    LLM --> Parse[JSON Parse]
    Parse --> AT[AgentTurn]
```

---

← [README](README.md) | → [02_System_Prompt](02_System_Prompt.md)
