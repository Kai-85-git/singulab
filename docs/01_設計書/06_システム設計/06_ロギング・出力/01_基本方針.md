# 01_基本方針

- **ログが命**(議事録 §7)。「後から何が起きたか追える」を最優先
- 可視化(映像)は提出物として強いが、**映像だけでは創発を立証できない**ので数値ログを常に残す
- 出力はすべて `output/{scenario}_{seed}_{timestamp}/` 以下に集約

---

← [README](README.md) | → [02_出力ファイル一覧](02_出力ファイル一覧.md)
