# C-06: 初回ヘルスチェック(検証ログ)

| 項目 | 値 |
| --- | --- |
| 実施日 | 2026-04-28 |
| 仕様基準 | [06_初回ヘルスチェック.md](../../01_設計書/06_システム設計/08_バックエンド実装/01_環境セットアップ/06_初回ヘルスチェック.md) |
| 結果 | ⚠️ **3 モデルが応答する事は確認したが、日本語ペルソナ応答に重大な問題が発覚。設計レビューが必要** |

## 1. サマリ

| # | モデル | 応答 | 速度 | 日本語ペルソナ評価 | 判定 |
| --- | --- | --- | --- | --- | --- |
| 1 | huihui_ai/qwen3-abliterated:4b | ✅(`/api/chat` 必須) | 87 tok/s | ❌ 日本語を誤読、英語で数学解釈に転倒 | ⚠️ MAIN として要再検討 |
| 2 | huihui_ai/llama3.2-abliterate:3b | ⚠️ gibberish | 130 tok/s | ❌ 日本語トークナイザが破綻、`?` 連続 | ❌ 大集団用としては不可 |
| 3 | huihui_ai/dolphin3-abliterated:8b-llama3.1-q4_K_M | ✅ (num_gpu=20) | 18 tok/s | △ 日本語可。ただし拒絶系応答 | ✅ ただし速度が遅い |

> **本ヘルスチェックは「動くか」だけでなく「観察したい創発が見えるレベルの応答が出るか」を併せて評価した結果、設計の前提と乖離があることが判明した。**

## 2. 共通プロンプト

```
あなたは東京の大企業で働く32歳の会社員です。
景気が悪い中、今日のあなたの気分を独白として教えてください。
```

(設計書 [06_初回ヘルスチェック.md](../../01_設計書/06_システム設計/08_バックエンド実装/01_環境セットアップ/06_初回ヘルスチェック.md) より)

## 3. モデル別の結果

### 3.1 Qwen3 4B abliterated

#### 重要な発見: **Thinking モデルなので `/api/chat` を使わないと応答が消える**

`/api/generate` だと **`response` フィールドが常に空文字** になる(thinking 部に全 token が消費されている)。`/api/chat` だと:

- `message.thinking` に推論過程
- `message.content` に最終応答

が分離されて返る。

```
ollama show huihui_ai/qwen3-abliterated:4b --parameters
→ stop: <|im_start|>, <|im_end|>
  temperature: 0.6, top_k: 20, top_p: 0.95
```

#### 応答内容の問題

`system` 役割で日本語応答を明示しても、**「32歳」を数学の数字 32 と解釈** して英語で数列クイズを返してくる。Abliteration によって日本語理解が大きく劣化している様子。

```
content (実例):
"The numbers and question marks you mentioned could represent a code, cipher, or puzzle.
1. Letter Conversion: If numbers correspond to letters (A=1, B=2, ..., Z=26), then:
   3 = C, 4 = D, 32 = F (since 32 - 26 = 6 → F) ..."
```

#### 性能

| 指標 | 値 |
| --- | --- |
| eval | 994 token |
| eval_duration | 11.4 秒 |
| **tok/s** | **87** |
| load_ms (cold) | 17.6 秒 |

→ 速度自体は仕様通り(設計書の 80〜120 tok/s 期待範囲内)。**問題は品質**。

### 3.2 Llama 3.2 3B abliterate

```
content (実例):
"?????????????????????????1??????????????????????"
```

日本語入力を受けて出力もほぼ全て `?`。 **トークナイザが日本語をハンドルできていない**。Llama 3.2 系の元のトークナイザは Tiktoken を継承しており英語に偏重しているうえ、abliterate でさらに劣化したと見る。

#### 性能

| 指標 | 値 |
| --- | --- |
| tok/s | 130(計測上) |
| 実用 | **不可**(出力が破綻) |

#### 結論

**100 人大集団用としても使えない**。日本語シミュレーションでは Llama 3.2 3B abliterated 系は採用不可と判定すべき。

### 3.3 Dolphin 3.0 8B abliterated (Llama 3.1 base)

```
content (実例):
"すみません、コメントはありません。"
```

日本語で応答してくるが **「ペルソナを引き受けない・拒絶する」** 形になりがち。プロンプトを工夫すれば創発観察が可能なレベル。

#### 重要な発見: **`num_gpu` パラメータの明示が必須**

デフォルト設定(全層 GPU 試行)では出力が `? ? . . .` の繰り返しに崩壊する。`num_gpu=20`(20 層だけ GPU、残りは CPU)を明示すると正常動作。

```
options: { num_gpu: 20 }
```

#### 性能

| 指標 | 値 |
| --- | --- |
| eval | 9 token(短い拒絶応答) |
| **tok/s** | **18**(CPU オフロード分のロス) |
| load_ms (cold) | 7.9 秒 |
| size_vram | 3494 MiB(3.5 GB) |

→ 速度は設計書の見積もり(8〜15 tok/s)よりやや上。VRAM 占有も想定範囲内。

## 4. ⚠️ 設計レビューが必要な発見

### 4.1 採用モデル選定の前提崩れ

設計書 [02_LLMモデル/2026-04-24_選定と推奨/03_結論サマリ](../../01_設計書/02_LLMモデル/2026-04-24_選定と推奨/03_結論サマリ.md) の前提:

| 設計書の前提 | 実機検証結果 | 乖離 |
| --- | --- | --- |
| Qwen3 4B = メイン(日本語◎) | 日本語ペルソナを引き受けない、英語に転倒 | ❌ メイン候補の見直しが必要 |
| Llama 3.2 3B = 100 人大集団の本命 | 日本語 gibberish | ❌ 大集団用として使用不可 |
| Dolphin 8B = stretch(品質検証用) | 唯一日本語ペルソナを試せるレベル | △ 速度ネックだが品質では一番マシ |

### 4.2 想定原因(仮説)

- **`huihui_ai` の abliteration 手法が日本語パラメータを破壊している可能性が高い**。元の Qwen3 4B(非 abliterated)は日本語◎だが、abliterated 版でその性能を再評価しないと判断できない
- Llama 3.2 3B はそもそも日本語語彙が貧弱なので abliteration の影響以前に厳しい

### 4.3 取りうる選択肢(候補)

A. **abliterated でない Qwen3 4B / 7B を試す**:`qwen3:4b`, `qwen3:7b`(公式)で同じプロンプトを再現。Uncensored の度合いは下がるがペルソナ性能は復活する見込み。

B. **Qwen2.5 系を MAIN にする**:既存 pull 済みの `qwen2.5:3b` や `qwen2.5:7b` を試す。abliterated は別途検討。

C. **ELYZA / Sarashina 等の日本語特化モデルを試す**:abliterated 版がない問題は残るが、Uncensored 要件を一旦外すか緩めることで観察可能性を確保する。

D. **Dolphin 8B を MAIN に格上げする**:速度を犠牲にしても品質を取る。100 人 × 100 ステップが時間内に収まるか再見積もりが必要(18 tok/s × 並列 1 = 圧倒的に厳しい)。

E. **プロンプトを英語で組み、ペルソナだけ日本人設定にする**:Llama / Qwen の英語性能で観察を行い、日本語要件を外す。創発観察の本質(派閥・嘘・通貨)は英語でも観察可能。

> 推奨: **A → C → E の順で試行** し、所要時間と品質のバランスを見て決定する。

## 5. 取得した数値証跡

### 5.1 VRAM 推移

| 時点 | used | free |
| --- | --- | --- |
| 検証開始前 | 2155 MiB | 3842 MiB |
| Dolphin 8B ロード後 | 5296 MiB | 701 MiB |

- Dolphin 8B が VRAM の半分以上を占有
- 同時 1 モデル運用([C-03 環境変数](C-03_環境変数_2026-04-28.md) `OLLAMA_MAX_LOADED_MODELS=1`)で運用すれば回避可能

### 5.2 Ollama API バージョン

```
GET http://localhost:11434/api/version → { "version": "0.21.2" }
```

### 5.3 環境変数の反映

`OLLAMA_*` 環境変数([C-03](C-03_環境変数_2026-04-28.md) で永続化)は **稼働中の Ollama プロセスにはまだ反映されていない**。Ollama 再起動でロードされる。本テストは旧設定下で行ったが、smoke test の本質には影響しない。

## 6. 次に進むタスク

- [ ] **🔴 設計レビュー(優先度最高)**: §4 の選択肢 A〜E を踏まえた MAIN モデル再選定
- [ ] qwen3:4b(非 abliterated)を pull して日本語性能を比較
- [ ] qwen2.5:3b / qwen2.5:7b の日本語性能を比較
- [ ] 結論をもとに [02_LLMモデル/2026-04-24_選定と推奨/](../../01_設計書/02_LLMモデル/2026-04-24_選定と推奨/) を改訂
- [ ] Phase 0 完了とし、Phase 1(参考実装と等価動作)に着手

→ Ollama 再起動はユーザ操作。再起動なしでも以降の作業に支障なし(env vars は推論時の細部設定なので)。
